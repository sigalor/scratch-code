int a, b;

real abc()
{
	return (real)f();
}

void main()
{
	for(a=3; a<5; ++a)
	{
		b = a/4;
		if(~b++ == a)
		{
			a %= 3;
			break;
		}
		else
		{
			continue;
		}
		while(1)
			continue;
	}
}

int c(int d, real e)
{
	return ~(int)f();
}

string f()
{
	real d;
	d += (real)a;
	return (string)d;
}
