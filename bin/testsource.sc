int a, b;

real abc()
{
	return (real)f();
}

void main()
{
	for(a=3; a<5; ++a)
	{
		b = a/4;
		if(~b++ == a)
		{
			a %= 3;
			break;
		}
		else
		{
			continue;
		}
		while(true)
			continue;
	}
	
	for(;(bool)c(1, 4.5);) { }
}

int c(int d, real e)
{
	return ~(int)f();
}

string f()
{
	real d;
	d += (real)a;
	return (string)d;
	
	if(true)
	{
	
	}
	else if(true)
	{
	
	}
	else
	{
	
	}
}
